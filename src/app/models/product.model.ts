export interface Product {
  id: string;
  name: string;
  categoryId: string;
  article: string;
  statusId: string;
  price: number;
  producerId: string;
  discountPercent: number;
  weightInKilogram: number;
  photoPath: string;
  description: string;
  photoBase64: string;
}
